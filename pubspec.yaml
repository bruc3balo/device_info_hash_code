name: device_info_hash_code
description: Detect Device Based on H/W Info
version: 0.0.1
homepage: https://github.com/bruc3balo/device_info_hash_code

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  crypto: ^3.0.3
  freezed: ^2.4.1
  device_info_plus: ^9.1.0
  mplatform: ^0.0.2
  freezed_annotation: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  json_serializable: ^6.7.1
  build_runner: ^2.4.6

flutter:
